* project
  - name          = border1
  - default       = execute

  * property
    - file        = build.properties
  * property
    - name        = rootdir
    - value       = ../../src/java
  * property
    - name        = mainclass
    - value       = Border1

  # compile
  * target
    - name        = compile
    * javac
      - classpath = ${rootdir}
      - srcdir    = .
      - source    = 1.4

  # execute
  * target
    - name        = execute
    - depends     = compile
    * java
      - classname = ${mainclass}
      - output    = result.html
      - classpath = .:${rootdir}

  # clean
  * target
    - name         = clean
    * delete
      - file       = ${mainclass}.class
    * delete
      - file       = result.html
