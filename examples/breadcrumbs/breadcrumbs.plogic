element breadcrumbs {
  ##@stag;
  $i = 0;
  foreach ($breadcrumbs as $crumb) {
    @element_crumb;           ## print <a>...</a>
    @element_separator;       ## print ' &gt; '
  }
  ##@etag;
}

element crumb {
    $path = $crumb['path'];
    @stag;
    echo $crumb['name'];
    @etag;
}

element title {
  echo $title;
}
