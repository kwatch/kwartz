#menulist {
        plogic:  {
            foreach (menu in menulist) {
                @stag;
                @cont;
                @etag;
            }
        }
}

#menu {
        value:  menu[:label];
        attrs:  "href" menu[:url];
}
