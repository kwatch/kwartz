#stylesheet_link {
        // replace element with print statement
        plogic: {
            print(X(<%= stylesheet_link_tag 'scaffold' %>), "\n");
        }
}

#list_recipes {
        // set href attribute value with Rails function
        attrs:  "href" X(<%= url_for(:action=>'list') %>);
}
