#new_form {
  attrs:  "action"  X(<%= url_for(:action=>'create') %>);
}

#recipe_category {
  value:  category.name;
  attrs:  "value" category.id;
  plogic: {
    foreach (category in categories) {
      @stag;
      @cont;
      @etag;
    }
  }
}

