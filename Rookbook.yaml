
properties:

  - project    :   kwartz
  - release    :   0.0.0

variables:

recipes:
  - product:   :test
    method: |
        rm_rf 'test.log'
	Dir.glob('test/test-*.rb').each do |filename|
          sys "ruby #{filename} 2>&1 | tee -a test.log"
        end
