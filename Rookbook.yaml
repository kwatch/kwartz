
properties:
  - name:   base_testnames
    value:  [test-directives, test-compile, test-handlers]

recipes:
  - product:   :test
    method: |
        rm_rf 'test.log'
        $base_testnames.each do |name|
          sys "ruby test/#{name}.rb 2>&1 | tee -a test.log"
        end
