/**
 *  @(#) KwartzTest.java
 *  @Id  $Id$
 *  @copyright $Copyright$
 *  @release $Release$
 */
package kwartz;
import junit.framework.TestCase;
import junit.framework.TestSuite;

public class KwartzTest extends TestCase {
    public static void main(String[] args) {
        TestSuite suite = new TestSuite();
<% docs = context[:docs] %>
<% doc = docs[0] %>
<% classes = doc['classes'] %>
<% for klass in classes do %>
        suite.addTest(new TestSuite(<%= klass %>.class));
<% end %>
        junit.textui.TestRunner.run(suite);
    }
    
    // dummy
    public void test1() {
        assertTrue(true);
    }
}
